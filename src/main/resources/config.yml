ai:
  provider: openai
  openai:
    api-key: ""
    model: gpt-3.5-turbo
  grok:
    api-key: ""
    model: grok-4
  gemini:
    api-key: ""
    model: gemini-1.5-flash
    # Leave blank to use the default public Gemini endpoint pattern
    endpoint: ""
build:
  fallback-material: minecraft:dirt
config-copies:
  folder: config-copies
databases:
  file: databases.yml
